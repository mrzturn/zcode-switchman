{
  "mcpServers": {
    "switchman-routing": {
      "type": "stdio",
      "command": "node",
      "args": ["${ZCODE_PLUGIN_ROOT}/mcp/routing-server.mjs"],
      "enabled": true,
      "timeoutMs": 30000
    }
  }
}
