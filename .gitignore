# Private porting/handover notes — never publish
docs/

# User-local runtime state (breaker, failure log, family map, handovers)
state/
*.log

# Session workspace: intermediate artifacts + handover docs (this repo too)
.switchman/

node_modules/
.DS_Store
